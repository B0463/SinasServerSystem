import { Request, Response } from 'express';
import Controller from './controller';

class SystemController extends Controller {
    public async func(req: Request, res: Response): Promise<any> {
        
    }
}

export default SystemController;